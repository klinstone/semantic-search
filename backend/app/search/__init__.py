"""Семантический поиск.

Содержимое модулей не реэкспортируется на уровне пакета: SearchService
тянет ORM-модели и app.config, что несовместимо с лёгким импортом
``app.search`` в окружениях без сконфигурированной БД. Импорт по
полному пути: ``from app.search.service import SearchService``.
"""